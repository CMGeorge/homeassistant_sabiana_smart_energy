name: Release workflow

on:
  release:
    types:
      - published

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v6

      - name: Get release version
        id: version
        uses: home-assistant/actions/helpers/version@master

      - name: Validate data
        run: |
          manifestversion=$(jq -r '.version' ${{ github.workspace }}/custom_components/homeassistant_sabiana_smart_energy/manifest.json)
          if [ "$manifestversion" != "${{ steps.version.outputs.version }}" ]; then
            echo "The version in custom_components/homeassistant_sabiana_smart_energy/manifest.json was not correct"
            echo "$manifestversion"
            exit 1
          fi

      - name: Create zip file for the integration
        run: |
          cd "${{ github.workspace }}/custom_components/homeassistant_sabiana_smart_energy"
          zip homeassistant_sabiana_smart_energy.zip -r ./

      - name: Upload the zipfile as a release asset
        uses: softprops/action-gh-release@v2.5.0
        with:
          files: ${{ github.workspace }}/custom_components/homeassistant_sabiana_smart_energy/homeassistant_sabiana_smart_energy.zip
